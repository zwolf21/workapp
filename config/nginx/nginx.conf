upstream python {
    ip_hash;
    server python:8000;
}

server {
    listen 80;

    location /static {
        alias /code/static;
    }
    
    location /media {
	alias /code/media;
    }

    location / {
        proxy_pass http://python/;
    }

}
